import 'package:flutter/material.dart';

class AnalyticsSeller extends StatelessWidget {
  const AnalyticsSeller({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
